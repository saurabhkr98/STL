#include <bits/stdc++.h>
using namespace std;
int main()
{
    deque<int> dq = {13,18};
    dq.push_back(31);
    dq.push_front(90);
    dq.pop_back();
    dq.pop_front();
    dq.front();
    d.back();
    //rest functions are same as vector
    // end, rend, begin, rbegin, swap, clear, empty, size, insert
}